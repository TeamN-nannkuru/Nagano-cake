<div class='container'>
    <div class='row'>
        <div class='col-xs-9'>
            <div class='thema'>
                <p>注文日</p>
                <p>配送先</p>
                <p>注文商品</p>
                <p>支払金額</p>
                <p>ステータス</p>
                <p>注文詳細</p>
            </div>
            <div class='order'>
                <% @order.each do |order| %>
                        <%= order.day %>
                        <%= order.postal_code %>
                        <%= order.address %>
                        <%= order.name %>
                        <%= order.item %>
                        <%= order.billing_amount %>
                        <%= order.order_status %>
                    <% link_to public_order_path(order.id) do %>
                        <p>表示する</p>
                    <% end %>
    <!-- ここは後で確認　-->
                <% end %>
            </div>
        </div>
    </div>
</div>