<div><%= @item.name %></div>
<div><%= @item.description %></div>
<div>¥<%= @item.price %>(税込)</div>
<div><%= f.select :item_count %></div>
<div><%= link_to 'カートに入れる', #public_cart_item_path(@item), class: 'btn btn-success' %></div>
